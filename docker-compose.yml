version: '3'
services:
 db:
   image: mysql:latest
   container_name: py_mysql_refuge
   ports:
     - "3306:3306"
   environment:
     - MYSQL_ROOT_PASSWORD=superSecretPassword
     - MYSQL_DATABASE=animals
   volumes:
     - ./mysql_data:/var/lib/mysql